import { NAME } from './constants';

export const SET_CURRENT_USER = `${NAME}/SET_CURRENT_USER`;
export const SET_TOKEN = `${NAME}/SET_TOKEN`;
export const REQUEST_TIMELINE = `${NAME}/REQUEST_TIMELINE`;
